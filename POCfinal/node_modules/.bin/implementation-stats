#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../formulajs/bin/implementation-stats" "$@"
  ret=$?
else 
  node  "$basedir/../formulajs/bin/implementation-stats" "$@"
  ret=$?
fi
exit $ret
